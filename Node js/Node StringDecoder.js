// Node.js String Decoder

// The Node.js StringDecoder is used to decode buffer into string.It is similar to buffer.toString()
// but provides extra supports to UTF

// You need to use require ('string_decoder') to use stringDecoder module


// Node.js StringDecoder Methods
// StringDecoder class has two methods only

// 1 decoder.write(buffer) = It is used to return the decoded string

// 2 decoder.end() = It is used to return trailing bytes if any left in the buffer
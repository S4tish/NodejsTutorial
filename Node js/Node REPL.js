// The `term REPL stands the Read Eval Print and Loop.It specifies a computer environment like
// a window console or a linux shell where you can enter the commands and the system responds
// with an output in an interactive mode

// REPL Environment
// The Node.js node come bundled with REPL environment .Each part of the REPL environment has a 
// specific work

// Read: It reads user input: parse the input into javascript data-structure and stores in memory

// Eval: It takes and evaluates the data structure

// Print : It prints the result

// Loop : It loops the above command until user press ctrl-c twice